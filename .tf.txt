################################
# Terraform Settings
################################
terraform {
  required_version = ">= 1.3.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"   # Lock provider version for stability
    }
  }
}

################################
# AWS Provider
################################
provider "aws" {
  profile = "default"     # Uses ~/.aws/credentials
  region  = "us-east-1"  # virginia region
}